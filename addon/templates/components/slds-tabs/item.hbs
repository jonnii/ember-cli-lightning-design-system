<a onclick={{action 'onTabClicked'}} class={{if isScoped 'slds-tabs_scoped__link' 'slds-tabs_default__link'}} role="tab" aria-selected={{ariaSelected}}>
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{text}}
  {{/if}}
</a>
