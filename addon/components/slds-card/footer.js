import Component from '@ember/component';
import layout from '../../templates/components/slds-card/footer';

export default Component.extend({
  layout,
  tagName: 'footer',
  classNames: 'slds-card__footer'
});
